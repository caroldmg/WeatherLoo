# Aquí irán los archivos de db.json y demás

* json-server --watch /nombredelarchivo.json/
* se muestran en http://localhost:3000/

* por el momento dejo puesto el mismo archivo que estaba en el repositorio anterior 